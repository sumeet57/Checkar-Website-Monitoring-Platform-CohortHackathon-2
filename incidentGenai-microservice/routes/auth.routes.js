import { Router } from "express";

const router = Router();

// apply the authentication middleware for refresh and access token handling
// all the middleware regard routing is used here, validation, authentication.

export default router;
